<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sporthub5">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_sport_hub"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_sport_hub_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SportHub5">
        <activity android:name=".MainActivity" android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>